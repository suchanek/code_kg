feat: add GitHub Actions integration and enhanced report metadata

- Add `.github/actions/codekg-action/` composite action for automated CodeKG analysis on PRs and pushes to main
- Implement `_get_report_metadata()` to include generation timestamp, version, commit SHA, and branch in reports
- Upgrade all datetime handling to UTC with `datetime.datetime.now(datetime.timezone.utc)`
- Enhance CLI error handling with try-except and explicit exit codes
- Update project description in pyproject.toml
- Add new GitHub workflow job `codekg-analysis` that integrates the action with caching and artifact upload

This enables zero-setup, automatic code graph analysis on every PR and main push, with metadata tracking
for audit and debugging purposes.
